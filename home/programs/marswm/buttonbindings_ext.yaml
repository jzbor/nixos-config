# Root window actions
- modifiers: []
  button: 3
  targets: [root]
  action: !execute xdg-xmenu -m | xmenu | /bin/sh

- modifiers: [Mod4, Shift]
  button: 1
  targets: [window, frame, root]
  action: !execute rofi -show window
